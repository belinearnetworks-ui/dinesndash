<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DinesNDash - Fast Food Delivery</title>
    <meta name="description" content="Dine well, we dash. The fastest food delivery service in town.">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    
    <!-- Lucide Icons (Assets only) -->
    <script src="https://unpkg.com/lucide@latest"></script>

    <style>
        /* --- CSS VARIABLES & RESET --- */
        :root {
            --primary: #e11d48;       /* Rose-600 */
            --primary-hover: #be123c; /* Rose-700 */
            --primary-light: #fff1f2; /* Rose-50 */
            --dark: #111827;          /* Gray-900 */
            --gray: #6b7280;          /* Gray-500 */
            --light-gray: #f9fafb;    /* Gray-50 */
            --white: #ffffff;
            --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
            --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
            --radius-md: 0.75rem;
            --radius-lg: 1rem;
            --radius-xl: 1.5rem;
            --font-main: 'Plus Jakarta Sans', sans-serif;
            --transition: all 0.3s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: var(--font-main);
            color: var(--dark);
            background-color: var(--white);
            line-height: 1.5;
            overflow-x: hidden;
        }

        a { text-decoration: none; color: inherit; transition: var(--transition); }
        ul { list-style: none; }
        img { max-width: 100%; display: block; }
        button { cursor: pointer; border: none; font-family: inherit; }

        /* --- UTILITIES --- */
        .container {
            max-width: 1280px;
            margin: 0 auto;
            padding: 0 1.5rem;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 0.75rem 1.5rem;
            font-weight: 700;
            border-radius: 9999px;
            transition: var(--transition);
            gap: 0.5rem;
        }

        .btn-primary {
            background-color: var(--primary);
            color: var(--white);
            box-shadow: 0 4px 14px 0 rgba(225, 29, 72, 0.39);
        }

        .btn-primary:hover {
            background-color: var(--primary-hover);
            transform: translateY(-2px);
        }

        .btn-outline {
            background-color: transparent;
            border: 2px solid rgba(255,255,255,0.2);
            color: var(--white);
        }
        
        .section-title {
            text-align: center;
            margin-bottom: 3rem;
        }
        
        .section-label {
            color: var(--primary);
            font-weight: 700;
            text-transform: uppercase;
            font-size: 0.875rem;
            letter-spacing: 0.05em;
            display: block;
            margin-bottom: 0.5rem;
        }

        .section-heading {
            font-size: 2.25rem;
            font-weight: 800;
            color: var(--dark);
        }

        /* --- NAVBAR --- */
        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 1000;
            padding: 1rem 0;
            transition: var(--transition);
            background-color: transparent;
        }

        .navbar.scrolled {
            background-color: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            box-shadow: var(--shadow-sm);
            padding: 0.75rem 0;
        }

        .nav-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 1.5rem;
            font-weight: 700;
        }

        .logo-icon {
            background-color: var(--primary);
            color: var(--white);
            padding: 0.5rem;
            border-radius: 0.75rem;
            display: flex;
        }

        .nav-links {
            display: flex;
            gap: 2rem;
            align-items: center;
        }

        .nav-link {
            font-weight: 500;
            color: var(--gray);
        }

        .nav-link:hover { color: var(--primary); }

        .mobile-menu-btn {
            display: none;
            background: none;
            color: var(--dark);
        }

        /* --- HERO SECTION --- */
        .hero {
            position: relative;
            min-height: 100vh;
            display: flex;
            align-items: center;
            padding-top: 5rem;
            color: var(--white);
            overflow: hidden;
        }

        .hero-bg {
            position: absolute;
            inset: 0;
            z-index: -2;
        }

        .hero-bg img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .hero-overlay {
            position: absolute;
            inset: 0;
            background: linear-gradient(to right, rgba(0,0,0,0.9) 0%, rgba(0,0,0,0.5) 100%);
            z-index: -1;
        }

        .hero-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
        }

        .hero-content h1 {
            font-size: 4rem;
            line-height: 1.1;
            margin-bottom: 1.5rem;
            font-weight: 800;
        }

        .gradient-text {
            background: linear-gradient(to right, #f43f5e, #fb923c);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .hero-content p {
            font-size: 1.25rem;
            color: #e5e7eb;
            margin-bottom: 2rem;
            max-width: 500px;
        }

        .search-box {
            background: var(--white);
            padding: 0.5rem;
            border-radius: 9999px;
            display: flex;
            max-width: 500px;
            box-shadow: var(--shadow-lg);
        }

        .search-input-wrapper {
            position: relative;
            flex-grow: 1;
        }

        .search-input-wrapper svg {
            position: absolute;
            left: 1rem;
            top: 50%;
            transform: translateY(-50%);
            color: var(--primary);
        }

        .search-box input {
            width: 100%;
            padding: 1rem 1rem 1rem 3rem;
            border: none;
            outline: none;
            font-size: 1rem;
            border-radius: 9999px;
        }

        .floating-image-container {
            position: relative;
            animation: float 6s ease-in-out infinite;
        }

        .floating-image {
            width: 400px;
            height: 400px;
            object-fit: cover;
            border-radius: 2rem;
            border: 4px solid rgba(255,255,255,0.2);
            box-shadow: var(--shadow-lg);
            transform: rotate(6deg);
            transition: var(--transition);
        }

        .floating-image:hover { transform: rotate(0deg); }

        .floating-card {
            position: absolute;
            bottom: -20px;
            left: -20px;
            background: var(--white);
            padding: 1rem;
            border-radius: 1rem;
            display: flex;
            align-items: center;
            gap: 1rem;
            color: var(--dark);
            box-shadow: var(--shadow-lg);
            animation: bounce 3s infinite;
        }

        /* --- STATS --- */
        .stats-section {
            padding: 3rem 0;
            border-bottom: 1px solid #e5e7eb;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            text-align: center;
        }

        .stat-item {
            border-right: 1px solid #e5e7eb;
        }
        
        .stat-item:last-child { border-right: none; }

        .stat-number {
            font-size: 2.25rem;
            font-weight: 800;
            color: var(--primary);
        }

        /* --- HOW IT WORKS --- */
        .section { padding: 6rem 0; }
        .bg-gray { background-color: var(--light-gray); }

        .steps-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2.5rem;
        }

        .step-card {
            background: var(--white);
            padding: 2.5rem;
            border-radius: var(--radius-xl);
            text-align: center;
            box-shadow: var(--shadow-sm);
            transition: var(--transition);
        }

        .step-card:hover {
            transform: translateY(-10px);
            box-shadow: var(--shadow-lg);
        }

        .step-icon {
            width: 5rem;
            height: 5rem;
            background: var(--primary-light);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1.5rem;
            color: var(--primary);
        }

        /* --- MENU GRID --- */
        .menu-header {
            display: flex;
            justify-content: space-between;
            align-items: flex-end;
            margin-bottom: 3rem;
        }

        .food-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
        }

        .food-card {
            background: var(--white);
            border-radius: var(--radius-lg);
            overflow: hidden;
            box-shadow: var(--shadow-lg);
            transition: var(--transition);
            position: relative;
        }

        .food-card:hover { transform: translateY(-5px); }

        .food-img-wrapper {
            height: 200px;
            overflow: hidden;
        }

        .food-img-wrapper img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s;
        }

        .food-card:hover .food-img-wrapper img { transform: scale(1.1); }

        .fav-btn {
            position: absolute;
            top: 1rem;
            right: 1rem;
            background: var(--white);
            padding: 0.5rem;
            border-radius: 50%;
            display: flex;
            color: #d1d5db;
        }

        .fav-btn:hover { color: var(--primary); }

        .food-info { padding: 1.5rem; }

        .food-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 0.5rem;
        }

        .price-tag {
            background: #dcfce7;
            color: #15803d;
            font-weight: 700;
            font-size: 0.8rem;
            padding: 0.25rem 0.5rem;
            border-radius: 0.5rem;
        }

        .btn-cart {
            width: 100%;
            margin-top: 1rem;
            background: #f3f4f6;
            color: var(--dark);
        }

        .btn-cart:hover {
            background: var(--primary);
            color: var(--white);
        }

        /* --- FEATURES --- */
        .features-section {
            background-color: var(--dark);
            color: var(--white);
            position: relative;
            overflow: hidden;
        }

        .features-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
            position: relative;
            z-index: 2;
        }

        .feature-item {
            display: flex;
            gap: 1.5rem;
            margin-bottom: 2rem;
        }

        .feature-icon {
            background: #1f2937;
            padding: 0.75rem;
            border-radius: 0.5rem;
            color: var(--primary);
            height: fit-content;
        }

        .glow {
            position: absolute;
            width: 300px;
            height: 300px;
            border-radius: 50%;
            filter: blur(80px);
            opacity: 0.2;
        }
        .glow-1 { top: 0; right: 0; background: var(--primary); }
        .glow-2 { bottom: 0; left: 0; background: blue; }

        /* --- APP DOWNLOAD --- */
        .app-card {
            background: var(--primary);
            border-radius: 3rem;
            padding: 4rem;
            color: var(--white);
            overflow: hidden;
            position: relative;
        }

        .app-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 3rem;
            align-items: center;
            position: relative;
            z-index: 2;
        }

        .store-buttons {
            display: flex;
            gap: 1rem;
            margin-top: 2rem;
        }

        .btn-store {
            background: var(--dark);
            color: var(--white);
            border-radius: 0.75rem;
            padding: 0.75rem 1.25rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
            transition: var(--transition);
        }

        .btn-store:hover { transform: scale(1.05); }

        .phone-mockup {
            width: 250px;
            height: 500px;
            background: var(--dark);
            border-radius: 2.5rem;
            border: 8px solid var(--dark);
            overflow: hidden;
            margin: 0 auto;
            position: relative;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
        }

        /* --- FOOTER --- */
        footer {
            padding: 4rem 0 2rem;
            border-top: 1px solid #e5e7eb;
            background: var(--white);
        }

        .footer-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 3rem;
            margin-bottom: 3rem;
        }

        .social-links { display: flex; gap: 1rem; margin-top: 1rem; }
        .social-links a { color: #9ca3af; }
        .social-links a:hover { color: var(--primary); }

        /* --- MODALS & TOASTS --- */
        .toast {
            position: fixed;
            bottom: 2rem;
            right: 2rem;
            background: var(--dark);
            color: var(--white);
            padding: 1rem 1.5rem;
            border-radius: 1rem;
            display: flex;
            align-items: center;
            gap: 1rem;
            box-shadow: var(--shadow-lg);
            z-index: 2000;
            transform: translateY(150%);
            transition: var(--transition);
        }
        
        .toast.show { transform: translateY(0); }

        .modal-overlay {
            position: fixed;
            inset: 0;
            background: rgba(0,0,0,0.7);
            z-index: 2000;
            display: flex;
            justify-content: center;
            align-items: center;
            opacity: 0;
            pointer-events: none;
            transition: var(--transition);
        }

        .modal-overlay.open {
            opacity: 1;
            pointer-events: all;
        }

        .modal {
            background: var(--white);
            padding: 2rem;
            border-radius: 1rem;
            width: 90%;
            max-width: 400px;
            transform: scale(0.9);
            transition: var(--transition);
        }

        .modal-overlay.open .modal { transform: scale(1); }

        .form-group { margin-bottom: 1rem; }
        .form-input {
            width: 100%;
            padding: 0.75rem;
            border: 1px solid #d1d5db;
            border-radius: 0.5rem;
            font-size: 1rem;
        }
        .form-input:focus {
            border-color: var(--primary);
            outline: none;
        }

        /* --- ANIMATIONS --- */
        @keyframes float {
            0%, 100% { transform: translateY(0) rotate(6deg); }
            50% { transform: translateY(-20px) rotate(6deg); }
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        .reveal {
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.8s ease-out;
        }
        
        .reveal.active {
            opacity: 1;
            transform: translateY(0);
        }

        /* --- MOBILE RESPONSIVE --- */
        @media (max-width: 968px) {
            .hero-content h1 { font-size: 3rem; }
            .hero-grid, .features-grid, .app-grid { grid-template-columns: 1fr; gap: 3rem; }
            .floating-image-container { display: none; }
            .stats-grid { grid-template-columns: 1fr 1fr; gap: 2rem; }
            .stat-item { border-right: none; }
            .nav-links { display: none; } /* Hide desktop menu */
            .mobile-menu-btn { display: block; }
            .app-card { padding: 2rem; }
            .store-buttons { flex-direction: column; }
        }

        /* Mobile Menu Drawer */
        .mobile-menu {
            position: fixed;
            top: 0;
            right: 0;
            bottom: 0;
            width: 100%;
            background: var(--white);
            z-index: 1001;
            padding: 2rem;
            transform: translateX(100%);
            transition: transform 0.3s ease-in-out;
            display: flex;
            flex-direction: column;
            gap: 2rem;
        }
        .mobile-menu.open { transform: translateX(0); }
        .mobile-menu a { font-size: 1.5rem; font-weight: 700; }
    </style>
</head>
<body>

    <!-- Navbar -->
    <nav class="navbar" id="navbar">
        <div class="container nav-content">
            <div class="logo" onclick="window.scrollTo(0,0)">
                <div class="logo-icon">
                    <i data-lucide="utensils-crossed"></i>
                </div>
                <span>Dines<span style="color: var(--primary)">N</span>Dash</span>
            </div>

            <div class="nav-links">
                <a href="#popular" class="nav-link">Menu</a>
                <a href="#how-it-works" class="nav-link">Stores</a>
                <a href="/signup" class="nav-link">Sign Up</a>
                <button onclick="openModal(); window.location.href='/signup'" class="btn btn-primary">Sign In</button>
            </div>

            <button class="mobile-menu-btn" onclick="toggleMobileMenu()">
                <i data-lucide="menu"></i>
            </button>
        </div>
    </nav>

    <!-- Mobile Menu Drawer -->
    <div class="mobile-menu" id="mobile-menu">
        <div style="display: flex; justify-content: flex-end;">
            <button onclick="toggleMobileMenu()"><i data-lucide="x" style="width: 32px; height: 32px;"></i></button>
        </div>
        <a href="#how-it-works" onclick="toggleMobileMenu()">Menu</a>
        <a href="#popular" onclick="toggleMobileMenu()">Stores</a>
        <a href="/signup" onclick="toggleMobileMenu()">Sign Up</a>
        <button onclick="openModal(); toggleMobileMenu(); window.location.href='/signup'" class="btn btn-primary" style="width: 100%">Sign In</button>
    </div>

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-bg">
            <!-- <img src="https://images.unsplash.com/photo-1504674900247-0877df9cc836?ixlib=rb-1.2.1&auto=format&fit=crop&w=1950&q=80" alt="Background"> -->
             <img src="/photo-1504674900247-0877df9cc836.avif" alt="">

            <div class="hero-overlay"></div>
        </div>
        
        <div class="container hero-grid">
            <div class="hero-content reveal active">
                <div style="display: inline-flex; align-items: center; gap: 0.5rem; background: rgba(255,255,255,0.1); padding: 0.5rem 1rem; border-radius: 2rem; margin-bottom: 1.5rem; border: 1px solid rgba(255,255,255,0.2);">
                    <span style="width: 8px; height: 8px; background: #4ade80; border-radius: 50%;"></span>
                    <span style="font-size: 0.875rem; font-weight: 500;">Delivering to your neighborhood</span>
                </div>
                
                <h1>You <span class="gradient-text">Dine</span>,<br>We Dash.</h1>
                <p>Craving something specific? We deliver your city's best food faster than you can set the table.</p>

                <div class="search-box">
                    <div class="search-input-wrapper">
                        
                        <input type="text" placeholder="What are you craving?">
                    </div>
                    <button class="btn btn-primary" onclick="searchLocation()">
                        Find Food <i data-lucide="arrow-right" style="width: 16px;"></i>
                    </button>
                </div>
            </div>
        </div>
    </section>

    <!-- Stats -->
    <div class="stats-section">
        <div class="container stats-grid">
            <div class="stat-item">
                <p class="stat-number">500+</p>
                <p style="color: var(--gray);">Restaurants</p>
            </div>
            <div class="stat-item">
                <p class="stat-number">30m</p>
                <p style="color: var(--gray);">Avg Delivery</p>
            </div>
            <div class="stat-item">
                <p class="stat-number">100k+</p>
                <p style="color: var(--gray);">Happy Eaters</p>
            </div>
            <div class="stat-item">
                <p class="stat-number">24/7</p>
                <p style="color: var(--gray);">Support</p>
            </div>
        </div>
    </div>

    <!-- How It Works -->
    <section id="how-it-works" class="section bg-gray">
        <div class="container">
            <div class="section-title reveal">
                <span class="section-label">Workflow</span>
                <h2 class="section-heading">Hungry to Happy in 3 Steps</h2>
            </div>

            <div class="steps-grid">
                <div class="step-card reveal">
                    <div class="step-icon"><i data-lucide="map-pin"></i></div>
                    <h3>1. Set Location</h3>
                    <p style="color: var(--gray); margin-top: 0.5rem;">Enter your location to see which restaurants are ready to dash to your door.</p>
                </div>
                <div class="step-card reveal">
                    <div class="step-icon"><i data-lucide="shopping-bag"></i></div>
                    <h3>2. Choose Order</h3>
                    <p style="color: var(--gray); margin-top: 0.5rem;">Browse menus from top rated local restaurants and add your favorites to the cart.</p>
                </div>
                <div class="step-card reveal">
                    <div class="step-icon"><i data-lucide="bike"></i></div>
                    <h3>3. Fast Delivery</h3>
                    <p style="color: var(--gray); margin-top: 0.5rem;">Track your food in real-time. We'll be there before the hunger pangs strike again.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Popular Grid -->
    <section id="popular" class="section">
        <div class="container">
            <div class="menu-header reveal">
                <div>
                    <span class="section-label">Menu</span>
                    <h2 class="section-heading">Trending Near You</h2>
                </div>
                <a href="#" style="color: var(--primary); font-weight: 700; display: flex; align-items: center; gap: 0.5rem;">View Full Menu <i data-lucide="arrow-right" style="width: 16px;"></i></a>
            </div>

            <div class="food-grid">
                <!-- Card 1 -->
                <div class="food-card reveal">
                    <div class="food-img-wrapper">
                        <!-- <img src="https://images.unsplash.com/photo-1568901346375-23c9450c58cd?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80" alt="Burger"> -->
                        <img src="/photo-1568901346375-23c9450c58cd.avif" alt="Burger">
                    </div>
                    <button class="fav-btn"><i data-lucide="heart" style="width: 20px;"></i></button>
                    <div class="food-info">
                        <div class="food-header">
                            <h3 style="font-weight: 700; font-size: 1.125rem;">Double Smash</h3>
                            <span class="price-tag">$12.99</span>
                        </div>
                        <p style="color: var(--gray); font-size: 0.875rem; margin-bottom: 1rem;">Double beef patty, cheddar cheese, pickles, and our secret dash sauce.</p>
                        <button class="btn btn-cart" onclick="addToCart('Double Smash')">Add to Cart</button>
                    </div>
                </div>

                <!-- Card 2 -->
                <div class="food-card reveal">
                    <div class="food-img-wrapper">
                        <!-- <img src="https://images.unsplash.com/photo-1574071318508-1cdbab80d002?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80" alt="Pizza"> -->

                        <img src="/photo-1574071318508-1cdbab80d002.avif" alt="Pizza">
                         
                    </div>
                    <button class="fav-btn"><i data-lucide="heart" style="width: 20px;"></i></button>
                    <div class="food-info">
                        <div class="food-header">
                            <h3 style="font-weight: 700; font-size: 1.125rem;">Pepperoni Dash</h3>
                            <span class="price-tag">$18.50</span>
                        </div>
                        <p style="color: var(--gray); font-size: 0.875rem; margin-bottom: 1rem;">Stone-baked crust, buffalo mozzarella, spicy pepperoni, and fresh basil.</p>
                        <button class="btn btn-cart" onclick="addToCart('Pepperoni Dash')">Add to Cart</button>
                    </div>
                </div>

                <!-- Card 3 -->
                <div class="food-card reveal">
                    <div class="food-img-wrapper">
                        <!-- <img src="https://images.unsplash.com/photo-1579871494447-9811cf80d66c?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80" alt="Sushi"> -->
                        <img src="/photo-1579871494447-9811cf80d66c.avif" alt="Sushi">
                    </div>
                    <button class="fav-btn"><i data-lucide="heart" style="width: 20px;"></i></button>
                    <div class="food-info">
                        <div class="food-header">
                            <h3 style="font-weight: 700; font-size: 1.125rem;">Salmon Set</h3>
                            <span class="price-tag">$24.00</span>
                        </div>
                        <p style="color: var(--gray); font-size: 0.875rem; margin-bottom: 1rem;">Fresh atlantic salmon nigiri, maki rolls, and spicy tuna crunch.</p>
                        <button class="btn btn-cart" onclick="addToCart('Salmon Set')">Add to Cart</button>
                    </div>
                </div>

                <!-- Card 4 -->
                <div class="food-card reveal">
                    <div class="food-img-wrapper">
                        <!-- <img src="https://images.unsplash.com/photo-1555126634-323283e090fa?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80" alt="Bowl"> -->
                        <img src="/photo-1555126634-323283e090fa.avif" alt="Bowl">

                    </div>
                    <button class="fav-btn"><i data-lucide="heart" style="width: 20px;"></i></button>
                    <div class="food-info">
                        <div class="food-header">
                            <h3 style="font-weight: 700; font-size: 1.125rem;">Vegan Power</h3>
                            <span class="price-tag">$15.50</span>
                        </div>
                        <p style="color: var(--gray); font-size: 0.875rem; margin-bottom: 1rem;">Quinoa, avocado, roasted chickpeas, kale, and tahini dressing.</p>
                        <button class="btn btn-cart" onclick="addToCart('Vegan Power')">Add to Cart</button>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Features -->
    <section id="features" class="features-section section">
        <div class="glow glow-1"></div>
        <div class="glow glow-2"></div>
        
        <div class="container features-grid">
            <div class="reveal">
                <span class="section-label" style="color: #fb7185;">Why DinesNDash?</span>
                <h2 class="section-heading" style="color: white; margin-top: 1rem; margin-bottom: 2rem;">We're more than just<br>a delivery service.</h2>
                <p style="color: #9ca3af; font-size: 1.125rem; margin-bottom: 2rem;">We built DinesNDash because waiting for food is the worst. We optimized every step of the logistics chain to ensure your fries are still crisp.</p>

                <div class="feature-item">
                    <div class="feature-icon"><i data-lucide="zap"></i></div>
                    <div>
                        <h4 style="font-size: 1.25rem; font-weight: 700;">Lightning Speed</h4>
                        <p style="color: #9ca3af;">Our AI routing finds the fastest path.</p>
                    </div>
                </div>
                <div class="feature-item">
                    <div class="feature-icon"><i data-lucide="shield-check"></i></div>
                    <div>
                        <h4 style="font-size: 1.25rem; font-weight: 700;">Secure Handling</h4>
                        <p style="color: #9ca3af;">Tamper-evident packaging guaranteed.</p>
                    </div>
                </div>
            </div>
            
            <div class="reveal">
                <!-- <img src="https://images.unsplash.com/photo-1526367790999-0150786686a2?ixlib=rb-1.2.1&auto=format&fit=crop&w=800&q=80" alt="Courier" style="border-radius: 1.5rem; border: 4px solid #1f2937;"> -->
                <img src="/photo-1526367790999-0150786686a2.avif" alt="Courier" style="border-radius: 1.5rem; border: 4px solid #1f2937;">

            </div>
        </div>
    </section>

    <!-- App Download -->
    <section id="download" class="section bg-gray">
        <div class="container">
            <div class="app-card">
                <div class="app-grid">
                    <div style="text-align: center; text-align: left;">
                        <h2 style="font-size: 2.5rem; font-weight: 800; margin-bottom: 1.5rem;">Food is better<br>in the app.</h2>
                        <p style="color: #ffe4e6; margin-bottom: 2rem;">Get exclusive deals, track your order in real-time, and get free delivery on your first 3 orders.</p>
                        
                        <div class="store-buttons">
                            <button class="btn-store">
                                <i data-lucide="apple" style="width: 32px; height: 32px;"></i>
                                <div style="text-align: left; line-height: 1.1;">
                                    <div style="font-size: 0.75rem;">Download on the</div>
                                    <div style="font-weight: 700; font-size: 1.25rem;">App Store</div>
                                </div>
                            </button>
                            <button class="btn-store">
                                <i data-lucide="play" style="width: 32px; height: 32px;"></i>
                                <div style="text-align: left; line-height: 1.1;">
                                    <div style="font-size: 0.75rem;">GET IT ON</div>
                                    <div style="font-weight: 700; font-size: 1.25rem;">Google Play</div>
                                </div>
                            </button>
                        </div>
                    </div>
                    
                    <div class="phone-mockup">
                        <img src="https://images.unsplash.com/photo-1512428559087-560fa5ce7d25?ixlib=rb-1.2.1&auto=format&fit=crop&w=400&q=80" style="width: 100%; height: 100%; object-fit: cover; opacity: 0.8;">
                        <div style="position: absolute; bottom: 2rem; left: 1rem; right: 1rem; background: rgba(255,255,255,0.9); padding: 1rem; border-radius: 1rem; backdrop-filter: blur(4px);">
                             <div style="display: flex; justify-content: space-between; font-weight: 700; color: #111827; margin-bottom: 0.5rem;">
                                 <span>Your Order</span>
                                 <span style="color: var(--primary);">$24.00</span>
                             </div>
                             <div style="height: 6px; background: #e5e7eb; border-radius: 99px; overflow: hidden;">
                                 <div style="height: 100%; width: 70%; background: var(--primary);"></div>
                             </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="footer-grid">
                <div>
                    <div class="logo" style="margin-bottom: 1rem;">
                        <div class="logo-icon"><i data-lucide="utensils-crossed" style="width: 20px;"></i></div>
                        <span>Dines<span style="color: var(--primary)">N</span>Dash</span>
                    </div>
                    <p style="color: var(--gray); font-size: 0.875rem;">Delicious food delivered to your door in record time.</p>
                </div>
                
                <div>
                    <h4 style="font-weight: 700; margin-bottom: 1rem;">Company</h4>
                    <ul style="color: var(--gray); font-size: 0.875rem; display: flex; flex-direction: column; gap: 0.5rem;">
                        <li><a href="#">About Us</a></li>
                        <li><a href="#">Careers</a></li>
                        <li><a href="#">Blog</a></li>
                    </ul>
                </div>
                
                 <div>
                    <h4 style="font-weight: 700; margin-bottom: 1rem;">Get in Touch</h4>
                    <div class="social-links">
                        <a href="#"><i data-lucide="facebook"></i></a>
                        <a href="#"><i data-lucide="twitter"></i></a>
                        <a href="#"><i data-lucide="instagram"></i></a>
                    </div>
                </div>
            </div>
            <div style="text-align: center; border-top: 1px solid #e5e7eb; padding-top: 2rem; color: var(--gray); font-size: 0.875rem;">
                &copy; 2023 DinesNDash Inc. All rights reserved.
            </div>
        </div>
    </footer>

    <!-- Toast -->
    <div id="toast" class="toast">
        <div style="background: #22c55e; padding: 0.25rem; border-radius: 50%; display: flex; align-items: center; justify-content: center;">
            <i data-lucide="check" style="width: 16px; height: 16px;"></i>
        </div>
        <div>
            <h4 style="font-weight: 700; font-size: 0.875rem;" id="toast-title">Success</h4>
            <p style="font-size: 0.75rem; color: #d1d5db;" id="toast-message">Action completed.</p>
        </div>
    </div>

    <!-- Modal -->
    <div id="auth-modal" class="modal-overlay">
        <div class="modal">
            <div style="text-align: center;">
                <div style="background: var(--primary-light); width: 50px; height: 50px; border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto 1rem; color: var(--primary);">
                    <i data-lucide="user"></i>
                </div>
                <h3 style="font-size: 1.5rem; font-weight: 700; margin-bottom: 0.5rem;">Join DinesNDash</h3>
                <p style="color: var(--gray); font-size: 0.875rem; margin-bottom: 1.5rem;">Sign up and get free delivery on your first order!</p>
                
                <div class="form-group"><input type="email" placeholder="Email Address" class="form-input"></div>
                <div class="form-group"><input type="password" placeholder="Password" class="form-input"></div>
                
                <button onclick="closeModal()" class="btn btn-primary" style="width: 100%; margin-bottom: 0.5rem;">Sign In</button>
                <button onclick="closeModal()" class="btn" style="width: 100%; color: var(--gray);">Cancel</button>
            </div>
        </div>
    </div>

    <script>
        // Init Icons
        lucide.createIcons();

        // Navbar Scroll
        const navbar = document.getElementById('navbar');
        window.addEventListener('scroll', () => {
            if (window.scrollY > 20) navbar.classList.add('scrolled');
            else navbar.classList.remove('scrolled');
        });

        // Mobile Menu
        const mobileMenu = document.getElementById('mobile-menu');
        function toggleMobileMenu() {
            mobileMenu.classList.toggle('open');
        }

        // Animation Observer
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) entry.target.classList.add('active');
            });
        }, { threshold: 0.1 });
        document.querySelectorAll('.reveal').forEach(el => observer.observe(el));

        // Toast Logic
        function showToast(title, message) {
            const toast = document.getElementById('toast');
            document.getElementById('toast-title').textContent = title;
            document.getElementById('toast-message').textContent = message;
            toast.classList.add('show');
            setTimeout(() => toast.classList.remove('show'), 3000);
        }

        function addToCart(item) { showToast("Added to Cart", `${item} has been added.`); }
        function searchLocation() { 
            const val = document.querySelector('.search-box input').value;
            if(!val) showToast("Error", "Enter an address");
            else showToast("Searching", `Finding food near ${val}`);
        }

        // Modal Logic
        const modal = document.getElementById('auth-modal');
        function openModal() { modal.classList.add('open'); }
        function closeModal() { modal.classList.remove('open'); }
        modal.addEventListener('click', (e) => {
            if (e.target === modal) closeModal();
        });
    </script>
</body>
</html>