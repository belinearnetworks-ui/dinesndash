<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Driver's Dashboard</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <style>
        @import url("https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap");

        :root {
            --primary: #e11d48;
            --primary-hover: #be123c;
            --dark: #0f172a;
            --gray-50: #f8fafc;
            --gray-100: #f1f5f9;
            --gray-200: #e2e8f0;
            --gray-600: #475569;
            --white: #ffffff;
            --sidebar-width: 260px;
            --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -2px rgba(0, 0, 0, 0.1);
            --success: #10b981;
            --warning: #f59e0b;
        }

        * { box-sizing: border-box; }

        body {
            font-family: "Plus Jakarta Sans", sans-serif;
            background-color: var(--gray-50);
            color: var(--dark);
            margin: 0;
            display: flex;
            min-height: 100vh;
        }

        /* --- Sidebar Styles --- */
        .sidebar {
            width: var(--sidebar-width);
            background: var(--dark);
            color: white;
            padding: 2rem 1.5rem;
            display: flex;
            flex-direction: column;
            position: fixed;
            height: 100vh;
        }

        .brand {
            font-size: 1.25rem;
            font-weight: 800;
            margin-bottom: 2.5rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .nav-links { list-style: none; padding: 0; margin: 0; }
        .nav-links li { margin-bottom: 0.5rem; }
        .nav-links a {
            display: flex; align-items: center; gap: 0.75rem;
            padding: 0.85rem 1rem; color: #94a3b8; text-decoration: none;
            border-radius: 0.75rem; font-weight: 600; transition: 0.2s;
        }
        .nav-links a:hover, .nav-links a.active { 
            background: rgba(255, 255, 255, 0.1); 
            color: white; 
        }
        .nav-links a.active { 
            background: var(--primary); 
            color: white;
        }

        /* --- Main Content --- */
        main {
            margin-left: var(--sidebar-width);
            flex-grow: 1;
            padding: 2.5rem;
            max-width: 1200px;
        }

        h2 { font-weight: 800; font-size: 1.75rem; margin-bottom: 2rem; }

        /* --- Table Styling --- */
        .table-container {
            background: var(--white);
            border-radius: 1rem;
            padding: 1.5rem;
            box-shadow: var(--shadow);
            overflow-x: auto;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            text-align: left;
        }

        th {
            padding: 1rem;
            color: var(--gray-600);
            font-weight: 700;
            border-bottom: 2px solid var(--gray-100);
            text-transform: uppercase;
            font-size: 0.75rem;
            letter-spacing: 0.05rem;
        }

        td {
            padding: 1.25rem 1rem;
            border-bottom: 1px solid var(--gray-100);
            font-size: 0.95rem;
            font-weight: 500;
        }

        tr:last-child td { border-bottom: none; }

        /* Badge for Status */
        .status-badge {
            padding: 0.4rem 0.8rem;
            border-radius: 2rem;
            font-size: 0.75rem;
            font-weight: 700;
            background: var(--gray-100);
            color: var(--gray-600);
        }

        .status-collected { background: #dcfce7; color: #166534; }

        /* --- Overlay/Modal Styles --- */
        #overlay {
            display: none;
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(15, 23, 42, 0.7);
            backdrop-filter: blur(4px);
            z-index: 1000;
            justify-content: center;
            align-items: center;
        }

        #deliveryForm {
            background: white;
            padding: 2.5rem;
            border-radius: 1.25rem;
            width: 90%;
            max-width: 450px;
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
            animation: modalPop 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
        }

        @keyframes modalPop {
            from { transform: scale(0.9); opacity: 0; }
            to { transform: scale(1); opacity: 1; }
        }

        #deliveryForm h2 { font-size: 1.5rem; margin-top: 0; text-align: center; }
        
        #deliveryForm input {
            width: 100%;
            padding: 0.75rem 1rem;
            margin: 1rem 0 1.5rem 0;
            border: 2px solid var(--gray-200);
            border-radius: 0.75rem;
            font-family: inherit;
            font-size: 1.1rem;
        }

        #SubmitDelivery {
            width: 100%;
            background: var(--primary);
            color: white;
            border: none;
            padding: 0.85rem;
            border-radius: 0.75rem;
            font-weight: 700;
            cursor: pointer;
            transition: 0.2s;
        }

        #SubmitDelivery:hover { background: var(--primary-hover); }

    </style>
</head>
<body>
    <aside class="sidebar">
        <div class="brand">
            <i class="fa-solid fa-truck-fast" style="color: var(--primary)"></i> 
            <span>FleetDriver</span>
        </div>
        <ul class="nav-links">
            <li><a href="/home/driver"><i class="fa-solid fa-chart-pie"></i> <span>Overview</span></a></li>
            <li><a href="/home/driver" ><i class="fa-solid fa-box"></i> <span>Available Orders</span></a></li>
            <li><a href="/driver/history" class="active"><i class="fa-solid fa-clock-rotate-left"></i> <span>My History</span></a></li>
            <li><a href="/driver/wallet"><i class="fa-solid fa-wallet"></i> <span>Earnings</span></a></li>
        </ul>
    </aside>

    <main>
        <h2>Order History</h2>
        <div class="table-container">
            <% if(history.length==0){%>
                <p style="text-align: center; color: var(--gray-600);">No order history found.</p>
            <%}else{%>
                <table>
                    <thead>
                        <tr>
                            <th>Date Of order</th>
                            <th>Order Status</th>
                            <th>Your earnings</th>
                            <th>User Rating</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% history.forEach(order=>{%>
                            <tr>
                                <td><%= order.accepted_at %></td>
                                <td><span class="status-badge <%= order.delivery_status === 'COLLECTED' ? 'status-collected' : '' %>"><%= order.delivery_status %></span></td>
                                <td style="color: var(--success); font-weight: 700;">R<%= order.Earnings %></td>
                                <td><i class="fa-solid fa-star" style="color: var(--warning)"></i> 4.8</td>
                            </tr>
                        <%}) %>
                    </tbody>
                </table>
            <%}%>
        </div>
    </main>

    <div id="overlay"></div>
</body>
</html>