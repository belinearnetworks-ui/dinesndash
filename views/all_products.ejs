<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Products For <%= Name %></title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />
    <style>
      @import url("https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap");

      :root {
        --primary: #e11d48;
        --primary-hover: #be123c;
        --dark: #0f172a;
        --gray-50: #f8fafc;
        --gray-100: #f1f5f9;
        --gray-200: #e2e8f0;
        --gray-600: #475569;
        --white: #ffffff;
        --sidebar-width: 260px;
        --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
      }

      body {
        font-family: "Plus Jakarta Sans", sans-serif;
        background-color: var(--gray-50);
        color: var(--dark);
        margin: 0;
        display: flex;
        min-height: 100vh;
      }

      /* --- SIDEBAR --- */
      .sidebar {
        width: var(--sidebar-width);
        background: var(--dark);
        color: white;
        padding: 1.5rem;
        display: flex;
        flex-direction: column;
        position: fixed;
        height: 100vh;
      }

      .brand {
        font-size: 1.25rem;
        font-weight: 800;
        margin-bottom: 2.5rem;
        display: flex;
        align-items: center;
        gap: 0.75rem;
      }

      .nav-links {
        list-style: none;
        padding: 0;
        margin: 0;
      }

      .nav-links li {
        margin-bottom: 0.5rem;
      }

      .nav-links a {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.85rem 1rem;
        color: #94a3b8;
        text-decoration: none;
        border-radius: 0.75rem;
        font-weight: 600;
        transition: 0.2s;
      }

      .nav-links a:hover,
      .nav-links a.active {
        background: rgba(255, 255, 255, 0.1);
        color: white;
      }

      .nav-links a.active {
        border-left: 4px solid var(--primary);
      }

      /* --- MAIN CONTENT --- */
      main {
        margin-left: var(--sidebar-width);
        flex: 1;
        padding: 2.5rem;
      }

      header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 2rem;
      }

      h1 {
        font-size: 1.75rem;
        font-weight: 800;
        margin: 0;
      }

      /* --- TABLE --- */
      .table-card {
        background: white;
        border-radius: 1rem;
        border: 1px solid var(--gray-200);
        box-shadow: var(--shadow);
        overflow: hidden;
      }

      table {
        width: 100%;
        border-collapse: collapse;
        text-align: left;
      }

      th {
        background: var(--gray-50);
        padding: 1rem 1.5rem;
        font-size: 0.75rem;
        text-transform: uppercase;
        color: var(--gray-600);
        font-weight: 800;
        letter-spacing: 0.05em;
      }

      td {
        padding: 1rem 1.5rem;
        border-bottom: 1px solid var(--gray-100);
        font-size: 0.95rem;
      }

      tr:hover td {
        background-color: #fff9fa;
      }

      /* --- MODAL --- */
      #modal-overlay {
        display: none;
        position: fixed;
        inset: 0;
        background: rgba(15, 23, 42, 0.7);
        backdrop-filter: blur(4px);
        z-index: 1000;
        justify-content: center;
        align-items: center;
        padding: 20px; /* Added padding for mobile breathing room */
      }

      .modal-card {
        background: white;
        padding: 2rem;
        border-radius: 1.25rem;
        width: 100%;
        max-width: 450px;
        box-shadow: var(--shadow);
        position: relative;

        /* SCROLLABLE FIX */
        max-height: 90vh;
        overflow-y: auto;
      }

      /* Custom scrollbar for the modal */
      .modal-card::-webkit-scrollbar {
        width: 6px;
      }
      .modal-card::-webkit-scrollbar-thumb {
        background-color: var(--gray-200);
        border-radius: 10px;
      }

      .modal-card h2 {
        margin-top: 0;
        font-weight: 800;
      }

      .form-group {
        margin-bottom: 1.25rem;
        display: flex;
        flex-direction: column;
        gap: 0.5rem;
      }
      .form-group label {
        font-size: 0.85rem;
        font-weight: 700;
        color: var(--gray-600);
      }
      .form-group input {
        padding: 0.75rem;
        border: 1px solid var(--gray-200);
        border-radius: 0.5rem;
        font-family: inherit;
        outline: none;
      }

      .btn-primary {
        background: var(--primary);
        color: white;
        border: none;
        padding: 0.85rem;
        border-radius: 0.75rem;
        font-weight: 700;
        cursor: pointer;
        width: 100%;
        margin-top: 1rem;
      }

      .btn-close {
        position: sticky; /* Changed to sticky so it stays at top while scrolling */
        float: right;
        top: 0;
        right: 0;
        background: white;
        border: none;
        font-size: 1.5rem;
        cursor: pointer;
        color: var(--gray-600);
        z-index: 10;
        line-height: 1;
      }

      /* --- INGREDIENT TAGS --- */
      .ingredients-container {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
        margin-top: 0.5rem;
        padding: 0.5rem;
        border: 1px solid var(--gray-200);
        border-radius: 0.5rem;
        min-height: 40px;
      }

      .ingredient-tag {
        background: var(--gray-100);
        color: var(--dark);
        padding: 0.3rem 0.75rem;
        border-radius: 2rem;
        font-size: 0.85rem;
        font-weight: 600;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        border: 1px solid var(--gray-200);
      }

      .add-ingredient-box {
        display: flex;
        gap: 0.5rem;
        margin-top: 0.5rem;
      }

      .btn-secondary {
        background: var(--dark);
        color: white;
        border: none;
        padding: 0 1rem;
        border-radius: 0.5rem;
        cursor: pointer;
      }

      @media (max-width: 900px) {
        .sidebar {
          width: 80px;
          padding: 1rem;
        }
        .sidebar .brand span,
        .nav-links span {
          display: none;
        }
        main {
          margin-left: 80px;
        }
      }
    </style>
  </head>
  <body>
    <aside class="sidebar">
      <div class="brand">
        <i class="fa-solid fa-store" style="color: var(--primary)"></i>
        <span><%= Name %></span>
      </div>
      <ul class="nav-links">
        <li>
          <a href="/Home"
            ><i class="fa-solid fa-chart-line"></i> <span>Dashboard</span></a
          >
        </li>
        <li>
          <a href="/all/products" class="active"
            ><i class="fa-solid fa-box-open"></i> <span>Products</span></a
          >
        </li>
        <li>
          <a href="/categories"
            ><i class="fa-solid fa-list"></i> <span>Categories</span></a
          >
        </li>
        <li>
          <a href="/full/store/orders"
            ><i class="fa-solid fa-cart-shopping"></i> <span>Orders</span></a
          >
        </li>
      </ul>
    </aside>

    <main>
      <h1>All <%= Name %> Meals</h1>
      <div class="table-card">
        <table>
          <tr>
            <th>#Pos</th>
            <th>Product Name</th>
            <th>Category</th>
            <th>Price</th>
            <th>Actions</th>
          </tr>
          <% Products.forEach((item,index)=>{%>
          <tr>
            <td><%= index+1 %></td>
            <td><%= item.meal_name %></td>
            <td><%= item.catagory_name %></td>
            <td>R <%= item.price %></td>
            <td>
              <select name="actions" id="<%= item.meal_id %>" class="actions">
                <option value="">Actions</option>
                <% if(item.availability=="AVAILABLE"){%>
                  <option value="NotAvailable">Not Available</option>
                  <%}else{%>
                    <option value="Available">Back in store</option>
                    <%} 
                    %>
                <option value="update">Update Meal info</option>
                <option value="delete" style="color: red">Delete Meal</option>
              </select>
            </td>
          </tr>
          <%}) %>
        </table>
      </div>

      <div id="modal-overlay">
        <div class="modal-card">
          <button class="btn-close" onclick="closeModal()">&times;</button>
          <div id="modal-body"></div>
        </div>
      </div>
    </main>

<script>
  const products = <%- JSON.stringify(Products) -%>;
  let currentIngredients = [];
  let currentProductId = null; // Track current product ID

  function closeModal() {
    document.getElementById("modal-overlay").style.display = "none";
    // Clean up the form event listener
    const form = document.getElementById("update-form");
    if (form) {
      form.replaceWith(form.cloneNode(true)); // Remove event listeners
    }
  }

  function renderIngredients() {
    const container = document.getElementById("ingredient-tags");
    const hiddenInput = document.getElementById("ingredients-serialized");
    if(!container || !hiddenInput) return;

    container.innerHTML = "";
    currentIngredients.forEach((ing, index) => {
      const tag = document.createElement("span");
      tag.className = "ingredient-tag";
      tag.innerHTML = `${ing} <i class="fa-solid fa-circle-xmark" onclick="removeIngredient(${index})"></i>`;
      container.appendChild(tag);
    });
    hiddenInput.value = JSON.stringify(currentIngredients);
  }

  function removeIngredient(index) {
    currentIngredients.splice(index, 1);
    renderIngredients();
  }

  function addIngredient() {
    const input = document.getElementById("new-ingredient-input");
    const val = input.value.trim();
    if (val) {
      currentIngredients.push(val);
      input.value = "";
      renderIngredients();
    }
  }

  // Handle form submission
  function handleFormSubmit(ev, productId) {
    ev.preventDefault();
    
    const form = document.getElementById("update-form");
    const formData = new FormData(form);
    
    fetch(`/update/product/${productId}`, {
      method: "POST",
      body: formData
    })
    .then(res => res.json())
    .then(data => {
      if (data.success) {
        alert("Product updated successfully!");
        window.location.reload();
      } else {
        alert("Error: " + data.message);
      }
    })
    .catch(err => {
      console.error("Fetch error:", err);
      alert("An error occurred while updating the product.");
    });
  }

  // Handle the Dropdown Actions
  document.querySelectorAll(".actions").forEach((el) => {
    el.addEventListener("change", (e) => {
      const id = e.target.id;
      const action = e.target.value;
      const product = products.find((c) => String(c.meal_id) === String(id));

      if (action === "update" && product) {
        currentProductId = id; // Store the current product ID
        
        // Safe parsing for Images and Ingredients
        let images = [];
        try { 
          images = JSON.parse(product.Images); 
        } catch(e) { 
          images = ['default.jpg']; 
        }
        
        try { 
          currentIngredients = JSON.parse(product.ingredients || "[]"); 
        } catch(e) { 
          currentIngredients = []; 
        }

        const modalBody = document.getElementById("modal-body");
        modalBody.innerHTML = `
          <h2>Update ${product.meal_name}</h2>
          <form id="update-form">
            <div class="form-group">
              <label>Current Image</label>
              <img src="/${images[0]}" style="width:100%; border-radius:10px; margin-bottom:10px;">
              <input type="file" name="product_pic">
            </div>
            <div class="form-group">
              <label>Product Name</label>
              <input type="text" name="product_name" value="${product.meal_name.replace(/"/g, '&quot;')}" required>
            </div>
            <div class="form-group">
              <label>Product Price (R)</label>
              <input type="number" name="product_price" value="${product.price}" step="0.01" required>
            </div>
            <div class="form-group">
              <label>Prep Time (Minutes)</label>
              <div style="display:grid; grid-template-columns: 1fr 1fr; gap:10px;">
                  <input type="number" name="from" value="${product.TimeFrom || ''}" placeholder="Min">
                  <input type="number" name="to" value="${product.TimeTo || ''}" placeholder="Max">
              </div>
            </div>
            
            <div class="form-group">
              <label>Ingredients</label>
              <div id="ingredient-tags" class="ingredients-container"></div>
              <div class="add-ingredient-box">
                <input type="text" id="new-ingredient-input" placeholder="e.g. Cheese">
                <button type="button" class="btn-secondary" onclick="addIngredient()">Add</button>
              </div>
              <input type="hidden" name="ingredients" id="ingredients-serialized">
            </div>
            <div class="form-group">
              <label>Store Safety Code</label>
              <input type="password" name="safety_code" placeholder="Confirm changes" required>
            </div>
            <button type="submit" class="btn-primary">Save Changes</button>
          </form>
        `;

        document.getElementById("modal-overlay").style.display = "flex";
        renderIngredients();

        // Attach submit handler with proper cleanup
        const form = document.getElementById("update-form");
        form.addEventListener("submit", (ev) => handleFormSubmit(ev, currentProductId));
      }

      if (action === "delete") {
        if (confirm(`Delete "${product.meal_name}" permanently?`)) {
          window.location.href = `/delete/product/${id}`;
        }
      }

      if(action=="NotAvailable"){
        el=`<h3>Please confirm that <i>${product.meal_name}</i>  is currently not available</h3>
        <form action='/meal/availability/NOT_AVAILABLE/${product.meal_id}' method="post">
          <label>Store Security Code Required For confirmation</label>
          <input type="number" placeholder='*****' id='security_code' name='security_code' required/>
          </br>
          </br>

          <button type='submit'>Confirm</button>
        </form>  
        `
        document.getElementById("modal-body").innerHTML=el;
        document.getElementById("modal-overlay").style.display = "flex";

      }else if(action=="Available"){
        el=`<h3>Please confirm that <i>${product.meal_name}</i> is now available</h3>
        <form action='/meal/availability/AVAILABLE/${product.meal_id}' method="post">
          <label>Store Security Code Required For confirmation</label>
          <input type="number" placeholder='*****' id='security_code' name='security_code' required/>
          </br>
          </br>

          <button type='submit'>Confirm</button>
        </form>  
        `
         document.getElementById("modal-body").innerHTML=el;
        document.getElementById("modal-overlay").style.display = "flex";
      }
      e.target.value = ""; // Reset dropdown
    });
  });

  // Close modal when clicking outside
  window.onclick = function(event) {
    const overlay = document.getElementById("modal-overlay");
    if (event.target == overlay) closeModal();
  }

  // Allow Enter key to add ingredient
  document.addEventListener('keypress', function(e) {
    if (e.key === 'Enter' && document.getElementById('new-ingredient-input') === document.activeElement) {
      e.preventDefault();
      addIngredient();
    }
  });
</script>
  </body>
</html>
