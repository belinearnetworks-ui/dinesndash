<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Driver's Dashboard</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />
    <style>
      @import url("https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap");

      :root {
        --primary: #e11d48;
        --primary-hover: #be123c;
        --dark: #0f172a;
        --gray-50: #f8fafc;
        --gray-100: #f1f5f9;
        --gray-200: #e2e8f0;
        --gray-600: #475569;
        --white: #ffffff;
        --sidebar-width: 260px;
        --shadow:
          0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -2px rgba(0, 0, 0, 0.1);
        --success: #10b981;
        --warning: #f59e0b;
      }

      * {
        box-sizing: border-box;
      }

      body {
        font-family: "Plus Jakarta Sans", sans-serif;
        background-color: var(--gray-50);
        color: var(--dark);
        margin: 0;
        display: flex;
        min-height: 100vh;
      }

      /* --- Sidebar Styles --- */
      .sidebar {
        width: var(--sidebar-width);
        background: var(--dark);
        color: white;
        padding: 2rem 1.5rem;
        display: flex;
        flex-direction: column;
        position: fixed;
        height: 100vh;
      }

      .brand {
        font-size: 1.25rem;
        font-weight: 800;
        margin-bottom: 2.5rem;
        display: flex;
        align-items: center;
        gap: 0.75rem;
      }

      .nav-links {
        list-style: none;
        padding: 0;
        margin: 0;
      }
      .nav-links li {
        margin-bottom: 0.5rem;
      }
      .nav-links a {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.85rem 1rem;
        color: #94a3b8;
        text-decoration: none;
        border-radius: 0.75rem;
        font-weight: 600;
        transition: 0.2s;
      }
      .nav-links a:hover,
      .nav-links a.active {
        background: rgba(255, 255, 255, 0.1);
        color: white;
      }
      .nav-links a.active {
        background: var(--primary);
        color: white;
      }

      /* --- Main Content --- */
      main {
        margin-left: var(--sidebar-width);
        flex-grow: 1;
        padding: 2.5rem;
        max-width: 1200px;
      }

      h2 {
        font-weight: 800;
        font-size: 1.75rem;
        margin-bottom: 2rem;
      }

      /* --- Table Styling --- */
      .table-container {
        background: var(--white);
        border-radius: 1rem;
        padding: 1.5rem;
        box-shadow: var(--shadow);
        overflow-x: auto;
      }

      table {
        width: 100%;
        border-collapse: collapse;
        text-align: left;
      }

      th {
        padding: 1rem;
        color: var(--gray-600);
        font-weight: 700;
        border-bottom: 2px solid var(--gray-100);
        text-transform: uppercase;
        font-size: 0.75rem;
        letter-spacing: 0.05rem;
      }

      td {
        padding: 1.25rem 1rem;
        border-bottom: 1px solid var(--gray-100);
        font-size: 0.95rem;
        font-weight: 500;
      }

      tr:last-child td {
        border-bottom: none;
      }

      /* Badge for Status */
      .status-badge {
        padding: 0.4rem 0.8rem;
        border-radius: 2rem;
        font-size: 0.75rem;
        font-weight: 700;
        background: var(--gray-100);
        color: var(--gray-600);
      }

      .status-collected {
        background: #dcfce7;
        color: #166534;
      }

      /* --- Overlay/Modal Styles --- */
      #overlay {
        display: none;
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(15, 23, 42, 0.7);
        backdrop-filter: blur(4px);
        z-index: 1000;
        justify-content: center;
        align-items: center;
      }

      #deliveryForm {
        background: white;
        padding: 2.5rem;
        border-radius: 1.25rem;
        width: 90%;
        max-width: 450px;
        box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
        animation: modalPop 0.3s cubic-bezier(0.34, 1.56, 0.64, 1);
      }

      @keyframes modalPop {
        from {
          transform: scale(0.9);
          opacity: 0;
        }
        to {
          transform: scale(1);
          opacity: 1;
        }
      }

      #deliveryForm h2 {
        font-size: 1.5rem;
        margin-top: 0;
        text-align: center;
      }

      #deliveryForm input {
        width: 100%;
        padding: 0.75rem 1rem;
        margin: 1rem 0 1.5rem 0;
        border: 2px solid var(--gray-200);
        border-radius: 0.75rem;
        font-family: inherit;
        font-size: 1.1rem;
      }

      #SubmitDelivery {
        width: 100%;
        background: var(--primary);
        color: white;
        border: none;
        padding: 0.85rem;
        border-radius: 0.75rem;
        font-weight: 700;
        cursor: pointer;
        transition: 0.2s;
      }

      #SubmitDelivery:hover {
        background: var(--primary-hover);
      }
      /* Header Styling */
      .page-header {
        margin-bottom: 2.5rem;
      }

      .subtitle {
        color: var(--gray-600);
        margin-top: -1.5rem;
        font-size: 0.95rem;
      }

      /* Wallet Layout */
      .wallet-grid {
        display: grid;
        grid-template-columns: 2fr 1fr; /* Main balance is wider */
        gap: 1.5rem;
        margin-bottom: 3rem;
      }

      /* The Main Balance Card */
      .balance-card {
        background: linear-gradient(135deg, var(--dark) 0%, #1e293b 100%);
        color: white;
        padding: 2rem;
        border-radius: 1.25rem;
        display: flex;
        justify-content: space-between;
        align-items: center;
        box-shadow: var(--shadow);
      }

      .balance-info {
        display: flex;
        align-items: center;
        gap: 1.5rem;
      }

      .wallet-icon {
        font-size: 2.5rem;
        background: rgba(225, 29, 72, 0.2);
        color: var(--primary);
        padding: 1rem;
        border-radius: 1rem;
      }

      .balance-info span {
        color: #94a3b8;
        font-size: 0.9rem;
        font-weight: 600;
        text-transform: uppercase;
        display: block;
        margin-bottom: 0.25rem;
      }

      .balance-info h3 {
        font-size: 2.5rem;
        margin: 0;
        font-weight: 800;
      }

      /* Withdraw Button */
      .withdraw-btn {
        background: var(--primary);
        color: white;
        text-decoration: none;
        padding: 1rem 1.5rem;
        border-radius: 0.75rem;
        font-weight: 700;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        transition:
          transform 0.2s,
          background 0.2s;
      }

      .withdraw-btn:hover {
        background: var(--primary-hover);
        transform: translateY(-2px);
      }

      /* Secondary Stat Card */
      .stat-card {
        background: var(--white);
        padding: 2rem;
        border-radius: 1.25rem;
        box-shadow: var(--shadow);
        display: flex;
        flex-direction: column;
        justify-content: center;
      }

      .stat-card span {
        color: var(--gray-600);
        font-weight: 600;
        font-size: 0.9rem;
      }

      .stat-card h4 {
        font-size: 1.5rem;
        margin: 0.5rem 0 0 0;
        color: var(--dark);
      }

      /* Responsive fix */
      @media (max-width: 900px) {
        .wallet-grid {
          grid-template-columns: 1fr;
        }

        .balance-card {
          flex-direction: column;
          align-items: flex-start;
          gap: 1.5rem;
        }

        .withdraw-btn {
          width: 100%;
          justify-content: center;
        }
      }
    </style>
  </head>
  <body>
    <aside class="sidebar">
      <div class="brand">
        <i class="fa-solid fa-truck-fast" style="color: var(--primary)"></i>
        <span>FleetDriver</span>
      </div>
      <ul class="nav-links">
        <li>
          <a href="/home/driver"
            ><i class="fa-solid fa-chart-pie"></i> <span>Overview</span></a
          >
        </li>
        <li>
          <a href="/home/driver" 
            ><i class="fa-solid fa-box"></i> <span>Available Orders</span></a
          >
        </li>
        <li>
          <a href="/driver/history"
            ><i class="fa-solid fa-clock-rotate-left"></i>
            <span>My History</span></a
          >
        </li>
        <li>
          <a href="/driver/wallet" class="active"
            ><i class="fa-solid fa-wallet"></i> <span>Earnings</span></a
          >
        </li>
      </ul>
    </aside>
    <main>
      <header class="page-header">
        <h2>Your Wallet</h2>
        <p class="subtitle">Manage your earnings and payouts</p>
      </header>

      <div class="wallet-grid">
        <div class="balance-card">
          <div class="balance-info">
            <i class="fa-solid fa-wallet wallet-icon"></i>
            <div>
              <span>Total Balance</span>
              <h3>R <%= Earnings.amount %></h3>
            </div>
          </div>
          <a href="/driver/withdraw" class="withdraw-btn">
            <i class="fa-solid fa-money-bill-transfer"></i> Withdraw Funds
          </a>
        </div>

        <div class="stat-card">
          <span>Pending Clearance</span>
          <h4>R 0.00</h4>
        </div>
      </div>

      <div class="recent-activity">
        <h3>Recent Payouts</h3>
        <div class="table-container">
          <p style="text-align: center; color: var(--gray-600); padding: 2rem">
            No recent withdrawals found.
          </p>
        </div>
      </div>
    </main>

    <div id="overlay"></div>
  </body>
</html>
